import hobbits from '../data/hobbit_data'

const initialState = {
  hobbits: hobbits
}


const reducer = (state = initialState, action) => {
  switch (action.type) {

  default:
    return state
  }
}

export default reducer
